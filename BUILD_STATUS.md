# Build Status — Krull D+A Billings

| Session | Description | Status | Notes |
|---------|-------------|--------|-------|
| S1 | Scaffolding + Database | Complete | Electron + React + Vite + Tailwind scaffold, SQLite with all tables and seed data, full IPC surface, three-panel layout shell |
| S2 | Clients + Projects | Complete | Full sidebar with dnd-kit group reordering, context menus, client CRUD with full form, project CRUD, client view with projects/line items panels, account tab, status bar |
| S3 | Line Items + Categories | Complete | Full line item CRUD with modal form (Details/Comments tabs, editable title, category/kind/billable dropdowns, duration, started/completed dates, rate/qty/due, markup/discount/tax, live total calculation), clickable rows with expand arrow, delete support, Categories settings panel with dnd-kit drag reorder, inline add/rename/delete |
| S4 | Invoices + Estimates | Complete | InvoiceModal (template picker, Invoice/Slips/Preview tabs, terms/due calc, retainer, delivery options, totals panel), EstimateModal (same structure adapted), Account tab (two-panel: transaction list with INV/EST/PAY badges + detail panel with rendered document view, PAID IN FULL stamp, category-grouped line items), AddPayment modal (pre-filled balance, method dropdown), PaymentReceipt (monospace receipt style), Send Invoice/Estimate buttons, getInvoices with paid_amount |
| S5 | Visual Document Builder | Complete | Builder with three-panel layout (block library, A4 canvas, properties panel), all 9 block types (header, client, doc_title, line_items, totals, notes, divider, spacer, text), dnd-kit drag from library to canvas + reorder, block selection with blue ring, delete on hover, per-block properties panel with all specified fields, line_items_block renders items grouped by category with bold CAPS headers, DocumentRenderer for previews, template management in Settings (list/edit/duplicate/delete/set default), save-as-template modal, two seeded default templates (KD Invoice 7 Days, KD Estimate), InvoiceModal and EstimateModal previews now use template blocks |
| S6 | PDF Generation + Email | Complete | Full pdf.js with renderToHtml (all 9 block types as HTML, line items grouped by category with bold CAPS header rows, brand colour backgrounds, PAID IN FULL red circular stamp overlay, logo as base64 embed, DD MMM YYYY dates, currency formatting), Puppeteer A4 PDF generation with 15mm margins and printBackground, email.js with Nodemailer SMTP send + Mac Mail.app fallback via shell.openPath, placeholder resolution in subject/body, main.js IPC: savePdfAs with dialog + file copy, printPdf/openPdf via shell, InvoiceModal and EstimateModal now execute delivery options (save/email/print/preview) after document creation |
| S7 | Settings (Full Preferences) | Complete | Full preferences panel with sidebar nav + SVG icons for all 8 tabs: General (date format, currency format, auto-save, default project status), Identity & Rates (logo upload/remove/preview, business name, ABN, address, phone, email, website, default hourly/mileage rates), Currencies (enable/disable 7 currencies with symbol/preview, set default), Taxes (add/delete tax rates with name + percentage, set default, seeded GST 10% + No Tax), Templates (existing from S5), Numbering (invoice/estimate prefix + next number with preview, reset buttons), Labels tab with 3 sub-tabs: Categories (existing from S3), Client Groups (dnd-kit sortable, add/rename/delete), Payment Methods (dnd-kit sortable, add/rename/delete, seeded 5 defaults), Email/SMTP (host/port/user/password with show/hide, from name/email, test connection, default invoice/estimate subject+body with placeholder list). Payment methods loaded dynamically in AddPayment modal |
| S8 | Dashboard + Polish + Packaging | Ready to Start | |
